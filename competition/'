/*
 *给定整数a1, a2, a3,...,an,判断是否可以从中选出若干数，使它们的和恰好为k.
 */

#include <iostream>
using namespace std;

int n, k;
int ans;
int a[100];

int dfs(int sum){

	return 0;
}

int main(){
	cin >> n >> k;
	for(int i = 0; i < n; i++)
		cin >> a[i];
	ans = 0;
	dfs(0);
	cout << ans << endl;
	return 0;
}
